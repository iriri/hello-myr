use std

use "lwt"
use "sched"

const main = {
	var w = "world\n"
	var world = {
		for ; ;
			std.put(w)
			lwt.yield()
		;;
	}
	lwt.init()
	lwt.spawn(hello)
	lwt.spawn(world)
	for ; ;
		std.put("\n")
		lwt.yield()
	;;
}

const hello = {
	for ; ;
		std.put("hello\n")
		lwt.yield()
	;;
}
